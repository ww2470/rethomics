#' @name dam_data
#' @title A simple rethomics data imported form a Trikinetics DAM2 file.
#' @description  A 32 animal, 5 days DAM2, single monitor data set formatted in a 
#' conventinal rethomics data.table (i.e. has \code{experiment_id}, \code{region_id} and \code{t} columns). 
#' A dummy variable, \code{condition}, was also added for illustration purposes.
#' @docType data
#' @usage dam_data
#' @format a \code{data.table} where each row is a single observation of one unique animal at one unique time
#' @source Gilestro lab, Imperial college, London
#' @author Anne Petzold, 2015-07-02
NULL
#' @name sleep_sexual_dimorphism
#' @title Sample of ethoscope data showing difference between males and females.
#' @description  A sleep-annotated 56 animal data set.Movement was defined for 10s time windows.
#' conventinal rethomics data.table (i.e. has \code{experiment_id}, \code{region_id} and \code{t} columns).
#' @docType data
#' @usage sleep_sexual_dimorphism
#' @format a \code{data.table} where each row is a single observation of one unique animal at one unique time
#' @source Gilestro lab, Imperial college, London
#' @author Quentin Geissmann, 2015-06-13
NULL
#' @name multiple_iterative_y_mazes
#' @title Unprocessed ethoscope data of animals waling in a y maze.
#' @description  An ,8 animal, single region_id data set formatted in a 
#' conventinal rethomics data.table (i.e. has \code{experiment_id}, \code{region_id} and \code{t} columns). 
#' @docType data
#' @usage multiple_iterative_y_mazes
#' @format a \code{data.table} where each row is a single observation of one unique animal at one unique time
#' @source Gilestro lab, Imperial college, London
#' @author Diana Bicazan, 2015-08-05
NULL
#' @name tube_monitor_validation
#' @title A dataset to validate tracking algorithms.
#' @description  A 20 animal  single monitor data set formatted in a 
#' conventinal rethomics data.table (i.e. has \code{experiment_id}, \code{region_id} and \code{t} columns). 
#' Only raw tracking values are present in dataset.
#' @docType data
#' @usage tube_monitor_validation
#' @format a \code{data.table} where each row is a single observation of one unique animal at one unique time
#' @source Gilestro lab, Imperial college, London
#' @author Quentin Geissmann, 2015-05-02
NULL
